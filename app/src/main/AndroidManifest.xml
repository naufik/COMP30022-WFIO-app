<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.navigation.wfio_dlyw.navigation">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Dark"
        android:usesCleartextTraffic="true"
        tools:replace="android:icon,android:theme">
        <activity
            android:name=".LogIn"
            android:label="Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ElderNavigation"
            android:label="@string/app_name"/>

        <activity
            android:name=".ElderSettings"
            android:label="@string/settings" />
        <activity
            android:name=".ElderHome"
            android:label="@string/home" />
        <activity
            android:name=".ChooseAccountType"
            android:label="@string/ChooseAccountType" />
        <activity
            android:name=".SignUp"
            android:label="@string/SignUp" />
        <activity
            android:name=".CarerHome"
            android:label="@string/home" />
        <activity
            android:name=".CarerConnect"
            android:label="@string/CarerConnect" />
        <activity
            android:name=".ElderConnect"
            android:label="@string/ElderConnect" />
        <activity
            android:name=".CarerSettings"
            android:label="@string/settings" />
        <activity
            android:name=".MyElders"
            android:label="@string/MyElders" />
        <activity
            android:name=".Favourites"  
            android:label="@string/favorites" />
        <activity
            android:name=".StoreClips"
            android:label="StoreClips" />
        <!--
             The API key for Google ElderMaps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity
            android:name=".ElderMaps"
            android:windowSoftInputMode="adjustPan"
            android:label="@string/title_activity_elder_maps"/>

        <activity
            android:name=".CarerMaps"
            android:label="@string/title_activity_carer_maps"
            android:windowSoftInputMode="adjustPan" />

        <receiver android:name=".NotificationReceiver" />

        <activity android:name=".AnswerHelp" />
        <activity android:name=".EnterCurrentPassword" />
        <activity android:name=".NewPassword" />
        <activity
            android:name=".ApplicationAppearance"
            android:label="@string/ApplicationAppearance" />
        <activity android:name=".MessageList" />
        <activity android:name=".MessageListElder" />


        <activity
            android:name=".MapsPermissionTest"
            android:label="@string/title_activity_maps_permission_test"></activity>
        <service android:name=".MsgUpdateService"/>
        <service android:name=".InitializeService"/>
        <service android:name="com.navigation.wfio_dlyw.comms.NotifyService" />
        <service android:name="com.navigation.wfio_dlyw.comms.NotificationService" />
        <service android:name="com.navigation.wfio_dlyw.comms.FirebaseNotificationService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <activity android:name=".CallActivity"></activity>
    </application>

</manifest>