<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.navigation.wfio_dlyw.navigation">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".LogIn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ElderNavigation" />
        <activity android:name=".ElderSettings" />
        <activity android:name=".ElderHome" />
        <activity android:name=".ChooseAccountType" />
        <activity android:name=".SignUp" />
        <activity android:name=".CarerHome" />
        <activity android:name=".CarerConnect" />
        <activity android:name=".CarerSettings"></activity>
    </application>

</manifest>